# Анализ проблем с распознаванием паттернов нейронной сетью

## 🔍 Результаты тестирования на MXH6 (20.10.2025 - 25.12.2025)

### Статистика найденных паттернов:
- **Нейронная сеть**: 68 паттернов (≥70% уверенность)
- **Математический сканер**: 0 паттернов
- **Правильная геометрия**: 0 из 20 проанализированных паттернов

## ❌ Обнаруженные проблемы

### 1. Неправильная последовательность индексов (100% случаев)
**Проблема**: Все проанализированные паттерны имеют нарушенную последовательность T0-T1-T2-T3-T4.

**Примеры**:
- Паттерн #1: `[9, 12, 11, 15, 20]` - T2 (11) идет после T1 (12)
- Паттерн #2: `[10, 44, 21, 29, 32]` - T2 (21) идет после T1 (44) и перед T3 (29)
- Паттерн #3: `[39, 61, 20, 39, 44]` - T2 (20) идет перед T0 (39)

**Причина**: Нейронная сеть предсказывает нормализованные координаты (x, y), но при обратном преобразовании в индексы свечей происходит ошибка, или модель не учитывает порядок точек.

### 2. T4 превышает 50% коррекции (45% случаев)
**Проблема**: Второй откат (T4) слишком глубокий относительно флагштока.

**Пример**: T4 = 273600.00, допустимый максимум = 272750.00

### 3. T4 ниже T0 (45% случаев)
**Проблема**: Для бычьего/медвежьего флага T4 должна быть выше T0, но часто находится ниже.

### 4. Флагшток слишком короткий (25% случаев)
**Проблема**: Высота флагштока (T0-T1) недостаточна для валидного паттерна.

### 5. Линии пересекают свечи (60% случаев)
**Проблема**: Линии тренда T1-T3 или T2-T4 пересекают тела или тени свечей, что нарушает логику паттерна.

## 📊 Анализ обучающих данных

- **Всего аннотаций**: 107
- **Правильная последовательность**: 103 (96.3%)
- **Неправильная последовательность**: 4 (3.7%)

**Вывод**: Обучающие данные в целом качественные, но есть единичные ошибки.

## 🔧 Возможные причины проблем

### 1. Проблема преобразования координат
При преобразовании нормализованных координат обратно в индексы свечей используется формула:
```python
candle_idx_in_window = x_norm * (window_size - 1)
```

**Проблема**: Модель предсказывает координаты независимо для каждой точки, не учитывая их порядок.

### 2. Модель не учитывает порядок точек
Текущая архитектура модели предсказывает координаты для T0-T4 параллельно, без учета того, что:
- T0 < T1 < T2 < T3 < T4 (по индексу)
- T0 и T1 должны быть на разных концах флагштока
- T2-T4 должны быть между T1 и T4

### 3. Недостаточное обучение
- Всего 104 размеченных примера
- Модель может быть недообучена
- Не хватает примеров без паттернов (class 0)

### 4. Sliding window создает перекрывающиеся паттерны
При анализе с sliding window (окно=100, шаг=10) одна и та же область графика анализируется несколько раз, что приводит к множественным предсказаниям одного паттерна.

## 💡 Рекомендации по улучшению

### 1. Исправить преобразование координат
Добавить валидацию порядка точек после предсказания:
```python
# После получения предсказаний от модели
pred_indices = [t0_idx, t1_idx, t2_idx, t3_idx, t4_idx]
if pred_indices != sorted(pred_indices):
    # Исправить порядок или отклонить паттерн
```

### 2. Добавить constraint в модель
Изменить архитектуру модели, чтобы она предсказывала относительные смещения вместо абсолютных координат:
- T0: абсолютная позиция
- T1: смещение от T0
- T2: смещение от T1
- T3: смещение от T2
- T4: смещение от T3

### 3. Улучшить обучающие данные
- Разметить больше примеров (150-200)
- Добавить примеры без паттернов (class 0)
- Проверить и исправить все 107 существующих аннотаций
- Добавить больше разнообразия (разные таймфреймы, разные инструменты)

### 4. Добавить постобработку
Создать функцию валидации предсказаний, которая:
- Проверяет последовательность индексов
- Проверяет геометрические условия
- Отклоняет невалидные паттерны

### 5. Использовать гибридный подход
- Математический сканер находит кандидаты
- Нейронная сеть фильтрует по качеству
- Это избежит проблем с неправильными координатами от NN

### 6. Увеличить количество эпох обучения
Текущая модель может быть недообучена. Попробовать:
- Увеличить количество эпох до 200-300
- Добавить data augmentation
- Использовать transfer learning

## 🎯 Немедленные действия

1. ✅ Добавить валидацию последовательности точек после предсказания
2. ✅ Создать функцию постобработки для фильтрации невалидных паттернов
3. ✅ Использовать гибридный подход (математика + фильтрация NN)
4. ⏳ Разметить больше данных (включая примеры без паттернов)
5. ⏳ Переобучить модель с учетом порядка точек

## 📈 Ожидаемые результаты после исправлений

- Правильная последовательность точек: >95%
- Соответствие геометрическим условиям: >80%
- Снижение ложных срабатываний: в 3-5 раз
- Повышение точности распознавания: с 0% до 60-80%

