# Как узнать, когда обучение закончится

## 📋 Способы проверки

### 1️⃣ Автоматический мониторинг с уведомлением

**Вариант A: Python скрипт (рекомендуется)**
```bash
cd complex_flag_scanner
python3 neural_network/monitor_training.py
```

**Вариант B: Bash скрипт**
```bash
cd complex_flag_scanner
./neural_network/notify_when_done.sh
```

Эти скрипты будут:
- ✅ Отслеживать процесс обучения
- ✅ Проверять обновления модели каждые 30 секунд
- ✅ Уведомлять о завершении (с системным уведомлением на macOS)
- ✅ Показывать финальную информацию о модели

### 2️⃣ Быстрая проверка статуса

```bash
cd complex_flag_scanner
./neural_network/CHECK_TRAINING_STATUS.sh
```

Покажет:
- Активен ли процесс обучения
- Время работы процесса
- Информацию о сохраненной модели
- Время последнего обновления

### 3️⃣ Ручная проверка

**Проверка процесса:**
```bash
ps aux | grep train_keypoints.py
```

Если процесс не найден → обучение завершено.

**Проверка модели:**
```bash
ls -lh neural_network/models/keypoint_model*.pth
stat -f "%Sm" neural_network/models/keypoint_model_best.pth
```

Если модель не обновлялась более 10 минут → обучение, вероятно, завершилось.

## ⏱️ Ожидаемое время

- **Всего эпох**: 100
- **Время на эпоху**: ~1-2 минуты
- **Общее время**: ~2-3 часа

## 🔔 Признаки завершения

1. ✅ Процесс `train_keypoints.py` отсутствует в `ps aux`
2. ✅ Модель `keypoint_model_best.pth` не обновлялась >10 минут
3. ✅ Размер модели стабилизировался
4. ✅ Появилось уведомление от скрипта мониторинга

## 📊 После завершения

Проверьте:
1. **Финальную модель**: `neural_network/models/keypoint_model_best.pth`
2. **Метрики обучения**: order_penalty должен быть близок к 0
3. **Тестирование**: запустите предсказания на новых данных

## 💡 Совет

Рекомендуется запустить мониторинг в отдельном терминале:
```bash
cd complex_flag_scanner
python3 neural_network/monitor_training.py
```

Оставьте его работать - он автоматически уведомит о завершении!

