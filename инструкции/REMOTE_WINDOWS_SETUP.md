# 🖥️ Настройка удаленной разработки на Windows через WSL2 + VS Code Remote SSH

## Содержание
1. [Установка WSL2](#1-установка-wsl2)
2. [Установка Docker Desktop](#2-установка-docker-desktop)
3. [Настройка SSH в WSL2](#3-настройка-ssh-в-wsl2)
4. [Настройка VS Code Remote SSH](#4-настройка-vs-code-remote-ssh)
5. [Клонирование проекта](#5-клонирование-проекта)
6. [Настройка окружения](#6-настройка-окружения)
7. [Запуск контейнеров](#7-запуск-контейнеров)
8. [Доступ к дашбордам](#8-доступ-к-дашбордам)
9. [Настройка автозапуска (для работы 24/7)](#9-настройка-автозапуска-для-работы-247)
10. [Мониторинг обучения](#10-мониторинг-обучения)
11. [Troubleshooting](#11-troubleshooting)

---

## 1. Установка WSL2

### Шаг 1.1: Проверка поддержки WSL2
Откройте PowerShell от имени администратора и выполните:
```powershell
wsl --status
```

Если WSL не установлен, увидите ошибку. Продолжайте.

### Шаг 1.2: Установка WSL2
Выполните в PowerShell (администратор):
```powershell
# Включить компонент WSL
dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart

# Включить компонент платформы виртуальных машин
dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart

# Перезагрузить компьютер
Restart-Computer
```

После перезагрузки:
```powershell
# Установить WSL2 как версию по умолчанию
wsl --set-default-version 2

# Установить Ubuntu (или другую дистрибуцию)
wsl --install -d Ubuntu-22.04
```

### Шаг 1.3: Первоначальная настройка Ubuntu
После установки Ubuntu откроется окно. Создайте пользователя:
- Введите имя пользователя (например, `yourname`)
- Введите пароль (запомните его!)
- Подтвердите пароль

### Шаг 1.4: Обновление системы
В окне Ubuntu выполните:
```bash
sudo apt update
sudo apt upgrade -y
```

---

## 2. Установка Docker Desktop

### Шаг 2.1: Скачивание и установка
1. Скачайте Docker Desktop для Windows: https://www.docker.com/products/docker-desktop/
2. Установите `Docker Desktop Installer.exe`
3. **Важно:** При установке включите опцию "Use WSL 2 instead of Hyper-V"

### Шаг 2.2: Настройка Docker Desktop
1. Запустите Docker Desktop
2. Перейдите в Settings → General
3. Убедитесь, что включено:
   - ✅ Use the WSL 2 based engine
   - ✅ Use Docker Compose V2

4. Перейдите в Settings → Resources → WSL Integration
5. Включите интеграцию с вашей Ubuntu:
   - ✅ Ubuntu-22.04 (или ваша версия)

### Шаг 2.3: Проверка установки
В окне Ubuntu выполните:
```bash
docker --version
docker compose version
```

Должны увидеть версии Docker и Docker Compose.

---

## 3. Настройка SSH в WSL2

### Шаг 3.1: Установка SSH-сервера
В Ubuntu (WSL2):
```bash
sudo apt update
sudo apt install openssh-server -y
```

### Шаг 3.2: Настройка SSH
Отредактируйте конфигурацию SSH:
```bash
sudo nano /etc/ssh/sshd_config
```

Найдите и измените/добавьте следующие строки:
```
Port 2222                    # Изменяем порт (22 может быть занят Windows)
PasswordAuthentication yes   # Разрешаем аутентификацию по паролю
PubkeyAuthentication yes     # Разрешаем ключи
PermitRootLogin no           # Запрещаем вход root
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 3.3: Запуск SSH-сервера
```bash
# Запустить SSH
sudo service ssh start

# Проверить статус
sudo service ssh status
```

### Шаг 3.4: Автозапуск SSH при старте WSL
Создайте файл для автозапуска:
```bash
sudo nano /etc/init.wsl
```

Добавьте:
```bash
#!/bin/bash
sudo service ssh start
```

Сделайте исполняемым:
```bash
sudo chmod +x /etc/init.wsl
```

Добавьте в `.bashrc`:
```bash
echo 'sudo service ssh start > /dev/null 2>&1' >> ~/.bashrc
```

---

## 4. Настройка VS Code Remote SSH

### Шаг 4.1: Установка расширения
В VS Code:
1. Откройте Extensions (Ctrl+Shift+X)
2. Найдите "Remote - SSH"
3. Установите расширение от Microsoft

### Шаг 4.2: Определение IP-адреса WSL2
На удаленной Windows машине узнайте IP WSL2:
```bash
# В Ubuntu (WSL2)
hostname -I
```

Вы увидите IP-адрес, например: `172.25.123.45`

Или используйте:
```bash
ip addr show eth0 | grep "inet " | awk '{print $2}' | cut -d/ -f1
```

**Важно:** IP-адрес WSL2 меняется при каждом перезапуске! Ниже будет решение.

### Шаг 4.3: Настройка SSH конфигурации в VS Code
1. В VS Code нажмите `F1` или `Ctrl+Shift+P`
2. Введите: `Remote-SSH: Open SSH Configuration File`
3. Выберите: `C:\Users\YourName\.ssh\config` (Windows)

Добавьте конфигурацию:
```
Host windows-remote
    HostName <IP_WSL2>           # Замените на IP из шага 4.2
    Port 2222
    User <yourname>               # Ваше имя пользователя в WSL2
    ForwardAgent yes
```

**Проблема с динамическим IP:** WSL2 меняет IP при перезапуске.

**Решение A - Статический IP (рекомендуется):**
Создайте файл `C:\Users\YourName\.wslconfig` в Windows:
```ini
[wsl2]
networkingMode=mirrored
dnsTunneling=true
firewall=true
autoProxy=true
```

**Решение B - Динамическое определение IP:**
В VS Code SSH config используйте скрипт для автоматического определения IP (см. шаг 4.4).

### Шаг 4.4: Подключение к удаленной машине
1. В VS Code: `F1` → `Remote-SSH: Connect to Host`
2. Выберите `windows-remote`
3. Введите пароль WSL2 пользователя
4. VS Code откроет новое окно, подключенное к WSL2

**Первый запуск:** VS Code установит серверную часть в WSL2 (это займет время).

---

## 5. Клонирование проекта

### Шаг 5.1: Установка Git в WSL2
В терминале VS Code (подключенном к WSL2):
```bash
sudo apt install git -y
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### Шаг 5.2: Клонирование репозитория
```bash
# Создать директорию для проектов
mkdir -p ~/projects
cd ~/projects

# Клонировать репозиторий
git clone <URL_ВАШЕГО_РЕПОЗИТОРИЯ>
# Или если репозиторий приватный:
# git clone git@github.com:username/repo.git

cd invest-python-main/complex_flag_scanner
```

### Шаг 5.3: Настройка Git (если нужно)
Если нужен доступ к приватным репозиториям:
```bash
# Генерация SSH ключа
ssh-keygen -t ed25519 -C "your.email@example.com"

# Показать публичный ключ
cat ~/.ssh/id_ed25519.pub

# Добавьте этот ключ в GitHub/GitLab
```

---

## 6. Настройка окружения

### Шаг 6.1: Копирование .env файла
```bash
# Создать .env файл
nano .env
```

Добавьте:
```
TINKOFF_INVEST_TOKEN=your_token_here
```

Сохраните: `Ctrl+O`, `Enter`, `Ctrl+X`

### Шаг 6.2: Проверка структуры проекта
```bash
ls -la
# Должны увидеть: docker-compose.yml, Dockerfile, .env, и т.д.
```

---

## 7. Запуск контейнеров

### Шаг 7.1: Сборка образов
```bash
docker compose build
```

Это займет время при первом запуске (скачивание базовых образов).

### Шаг 7.2: Запуск всех сервисов
```bash
docker compose up -d
```

Проверка статуса:
```bash
docker compose ps
```

Должны увидеть:
- `flag_scanner` - Running
- `flag_labeling` - Running
- `trading_bot` - Running
- `trading_dashboard` - Running

### Шаг 7.3: Просмотр логов
```bash
# Все логи
docker compose logs -f

# Логи конкретного сервиса
docker compose logs -f trading-bot
docker compose logs -f labeling
```

---

## 8. Доступ к дашбордам

### Шаг 8.1: Проброс портов через SSH
В VS Code SSH config (`~/.ssh/config` на вашей локальной машине):
```
Host windows-remote
    HostName <IP_WSL2>
    Port 2222
    User <yourname>
    ForwardAgent yes
    LocalForward 8504 localhost:8504    # Основной дашборд
    LocalForward 8505 localhost:8505    # Разметка
    LocalForward 8506 localhost:8506    # Trading dashboard
```

Или используйте VS Code Port Forwarding:
1. В VS Code: `F1` → `Remote-SSH: Forward Port from Active Host`
2. Введите порт (например, `8505`)
3. VS Code автоматически пробросит порт

### Шаг 8.2: Доступ через браузер
После проброса портов:
- **Разметка:** http://localhost:8505
- **Trading Dashboard:** http://localhost:8506

---

## 9. Настройка автозапуска (для работы 24/7)

### Шаг 9.1: Автозапуск Docker Desktop при загрузке Windows
1. Откройте Docker Desktop
2. Перейдите в Settings → General
3. Включите: ✅ **Start Docker Desktop when you log in**

### Шаг 9.2: Автозапуск контейнеров при старте WSL2

Создайте скрипт автозапуска в WSL2:
```bash
# Создать скрипт автозапуска
nano ~/start_services.sh
```

Содержимое:
```bash
#!/bin/bash
cd ~/projects/invest-python-main/complex_flag_scanner
docker compose up -d
```

Сделать исполняемым:
```bash
chmod +x ~/start_services.sh
```

Добавить в `.bashrc` (будет запускаться при входе в WSL2):
```bash
# Автозапуск сервисов (только если Docker доступен)
if command -v docker &> /dev/null && docker ps &> /dev/null; then
    cd ~/projects/invest-python-main/complex_flag_scanner 2>/dev/null && docker compose up -d > /dev/null 2>&1
fi
```

### Шаг 9.3: Настройка автозапуска WSL2 при загрузке Windows (опционально)

Создайте PowerShell скрипт для автозапуска WSL2 и сервисов:

1. Создайте файл `C:\Users\YourName\start_wsl_services.ps1`:
```powershell
# Запуск WSL2 и сервисов
wsl -d Ubuntu-22.04 -e bash -c "cd ~/projects/invest-python-main/complex_flag_scanner && docker compose up -d"
```

2. Добавьте в автозагрузку Windows:
   - Нажмите `Win+R`, введите `shell:startup`
   - Создайте ярлык на `start_wsl_services.ps1`
   - Или добавьте через Планировщик заданий (Task Scheduler)

### Шаг 9.4: Проверка автозапуска
```bash
# Перезагрузите компьютер и проверьте:
docker compose ps
# Все контейнеры должны быть в статусе "Up"
```

## 10. Мониторинг обучения

### Шаг 9.1: Запуск обучения нейросети
В терминале VS Code (подключенном к WSL2):
```bash
cd ~/projects/invest-python-main/complex_flag_scanner

# Запуск обучения в фоне
docker compose exec labeling python3 -u neural_network/train_keypoints.py \
    --epochs 100 \
    --batch_size 16 \
    --tolerance_normalized 0.003 > neural_network/training_log.txt 2>&1 &

# Мониторинг логов
tail -f neural_network/training_log.txt
```

### Шаг 9.2: Проверка использования ресурсов
```bash
# Использование CPU/памяти контейнерами
docker stats

# Использование ресурсов WSL2
wsl --list --verbose
```

### Шаг 9.3: Остановка/перезапуск контейнеров
```bash
# Остановить все
docker compose stop

# Запустить снова
docker compose start

# Перезапустить конкретный сервис
docker compose restart trading-bot

# Полная пересборка и перезапуск
docker compose up -d --build
```

---

## 11. Troubleshooting

### Проблема 1: SSH не подключается
**Решение:**
```bash
# Проверить, запущен ли SSH в WSL2
sudo service ssh status

# Перезапустить SSH
sudo service ssh restart

# Проверить порт
sudo netstat -tlnp | grep :2222
```

### Проблема 2: IP-адрес WSL2 меняется
**Решение:** Используйте PowerShell скрипт для автоматического определения IP:
```powershell
# Создайте файл get-wsl-ip.ps1
$wslIp = (wsl hostname -I).Trim()
Write-Host $wslIp
```

В VS Code SSH config можно использовать `ProxyCommand` (но это сложнее).

**Проще:** Используйте `localhost` если подключаетесь с той же машины.

### Проблема 3: Docker не работает в WSL2
**Решение:**
```bash
# Проверить, что Docker Desktop запущен в Windows
# В WSL2:
docker ps

# Если ошибка, проверьте настройки Docker Desktop:
# Settings → Resources → WSL Integration → Ubuntu включен
```

### Проблема 4: Порты не пробрасываются
**Решение:**
1. В VS Code: `F1` → `Remote-SSH: Kill VS Code Server on Host`
2. Переподключитесь
3. Или используйте явный проброс в SSH config (см. шаг 8.1)

### Проблема 5: Медленная работа
**Решение:**
1. Убедитесь, что файлы проекта находятся в файловой системе WSL2 (`~/projects`), а не на Windows диске (`/mnt/c/...`)
2. WSL2 работает быстрее с файлами внутри своей файловой системы

### Проблема 6: Обучение прерывается при отключении SSH
**Решение:**
Используйте `screen` или `tmux`:
```bash
# Установить screen
sudo apt install screen -y

# Запустить screen сессию
screen -S training

# Внутри screen запустить обучение
docker compose exec labeling python3 neural_network/train_keypoints.py --epochs 100

# Отключиться: Ctrl+A, затем D

# Подключиться обратно:
screen -r training
```

---

## Вспомогательные скрипты

В директории `scripts/` есть полезные скрипты для автоматизации:

### Скрипт проверки окружения
```bash
# Сделать исполняемым
chmod +x scripts/check_remote_setup.sh

# Запустить проверку
./scripts/check_remote_setup.sh
```

### Скрипт запуска обучения
```bash
# Сделать исполняемым
chmod +x scripts/start_training.sh

# Запустить обучение (100 эпох по умолчанию)
./scripts/start_training.sh

# Или с параметрами
./scripts/start_training.sh 200 32 0.003

# Подключиться к сессии обучения
screen -r nn_training

# Посмотреть логи без подключения к screen
tail -f neural_network/training_log.txt
```

### Скрипт управления контейнерами
```bash
# Сделать исполняемым
chmod +x scripts/manage_containers.sh

# Статус всех контейнеров
./scripts/manage_containers.sh status

# Запустить все
./scripts/manage_containers.sh start

# Перезапустить торгового робота
./scripts/manage_containers.sh restart trading-bot

# Логи конкретного сервиса
./scripts/manage_containers.sh logs labeling
```

---

## Дополнительные полезные команды

### Мониторинг обучения в реальном времени
```bash
# Просмотр последних 20 строк и обновление
watch -n 2 'tail -n 20 neural_network/training_log.txt'

# Или простой tail
tail -f neural_network/training_log.txt
```

### Проверка статуса всех контейнеров
```bash
docker compose ps
docker compose top
```

### Очистка Docker (если нужно место)
```bash
# Удалить неиспользуемые образы
docker image prune -a

# Удалить неиспользуемые контейнеры и volumes
docker system prune -a --volumes
```

### Резервное копирование данных
```bash
# Создать backup аннотаций
tar -czf backup_annotations_$(date +%Y%m%d).tar.gz neural_network/data/annotations.csv

# Создать backup моделей
tar -czf backup_models_$(date +%Y%m%d).tar.gz neural_network/models/
```

---

## Быстрый старт (после настройки)

```bash
# 1. Подключиться к WSL2 через VS Code Remote SSH

# 2. Перейти в проект
cd ~/projects/invest-python-main/complex_flag_scanner

# 3. Запустить все сервисы
docker compose up -d

# 4. Проверить логи
docker compose logs -f trading-bot

# 5. Открыть дашборд в браузере
# http://localhost:8505 (через проброс портов)
```

---

## Полезные ссылки

- [WSL2 документация](https://docs.microsoft.com/en-us/windows/wsl/)
- [Docker Desktop WSL2 backend](https://docs.docker.com/desktop/windows/wsl/)
- [VS Code Remote SSH](https://code.visualstudio.com/docs/remote/ssh)
- [Docker Compose документация](https://docs.docker.com/compose/)

---

**Готово!** Теперь у вас есть полноценная удаленная среда разработки на Windows.
