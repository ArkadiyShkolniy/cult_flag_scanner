# Удаление уже размеченных файлов

## Текущая реализация

**В режиме "Добавить координаты к существующим" уже реализована автоматическая фильтрация:**

- ✅ Показываются только файлы **БЕЗ координат** (для разметки)
- ✅ Файлы **С координатами** автоматически **скрыты** из списка
- ✅ Не нужно размечать их повторно

### Как это работает:

```python
# В labeling_dashboard.py
if 't0_idx' in annotations_list.columns:
    missing_coords = annotations_list['t0_idx'].isna()
    annotations_list = annotations_list[missing_coords].copy()  # Только без координат!
```

**Результат:** Файлы с координатами не показываются в списке выбора.

---

## Опции для управления файлами

### Вариант 1: Текущий (автоматическое скрытие) ✅ РЕКОМЕНДУЕТСЯ

**Преимущества:**
- ✅ Автоматически - ничего не нужно делать
- ✅ Безопасно - файлы не удаляются, просто скрыты
- ✅ Можно вернуть - если нужно, можно изменить фильтр

**Как работает:**
- Файлы с координатами автоматически не показываются
- Они остаются в annotations.csv, просто скрыты из списка

---

### Вариант 2: Показывать все файлы с опцией фильтра

Можно добавить переключатель "Показать все файлы" / "Только без координат":

```python
show_all = st.checkbox("Показать все файлы (включая размеченные)", value=False)

if not show_all:
    # Фильтруем только без координат
    annotations_list = annotations_list[missing_coords].copy()
```

**Преимущества:**
- ✅ Можно увидеть все файлы
- ✅ Можно переразметить, если нужно

**Недостатки:**
- ❌ Больше файлов в списке

---

### Вариант 3: Удаление записей из annotations.csv

Можно добавить кнопку "Удалить из списка" для каждого файла:

```python
if st.button("🗑️ Удалить из списка", key=f"delete_{idx}"):
    # Удаляем запись из annotations.csv
    annotations_df = annotations_df.drop(index=idx)
    annotator.annotations = annotations_df
    annotator.save_annotations()
```

**Преимущества:**
- ✅ Полное удаление из списка

**Недостатки:**
- ❌ Опасно - можно случайно удалить нужные данные
- ❌ Нет возможности восстановить
- ❌ Потеря данных о разметке

---

### Вариант 4: Помечать как "завершенный"

Можно добавить колонку `status` (pending/completed) и фильтровать по ней:

```python
# В annotations.csv добавляем колонку status
annotation['status'] = 'completed' if has_coords else 'pending'

# Фильтруем
annotations_list = annotations_list[annotations_list['status'] == 'pending']
```

**Преимущества:**
- ✅ Явное управление статусом
- ✅ Можно вернуть файл в разметку

**Недостатки:**
- ❌ Нужно изменять структуру данных

---

## Рекомендация

**Использовать текущий вариант (автоматическое скрытие):**

✅ **Почему:**
1. Уже работает - файлы с координатами автоматически скрыты
2. Безопасно - данные не удаляются
3. Просто - ничего дополнительного не нужно делать
4. Логично - если координаты есть, файл уже размечен

**Текущее поведение:**
- В режиме "Добавить координаты к существующим" показываются только файлы БЕЗ координат
- Файлы С координатами автоматически скрыты
- Не нужно размечать их повторно ✅

---

## Если нужно видеть все файлы

Можно добавить опцию показа всех файлов:

```python
show_completed = st.checkbox("Показать уже размеченные файлы", value=False)

if not show_completed:
    # Только без координат
    annotations_list = annotations_list[missing_coords].copy()
else:
    # Показать все
    pass
```

---

## Итог

**Текущая реализация уже решает задачу:**
- ✅ Файлы с координатами автоматически скрыты
- ✅ Не нужно размечать их повторно
- ✅ Безопасно - данные не удаляются

**Если нужно больше контроля:**
- Можно добавить опцию "Показать все файлы"
- Можно добавить статус "завершенный"
- Но удаление не рекомендуется (потеря данных)

