# 🏠 Настройка домашнего компьютера как удаленного сервера (WSL2 + VS Code)

## Краткое описание

Использование домашнего Windows компьютера с WSL2 как удаленного сервера для запуска торгового бота и обучения нейросети.

**Архитектура:**
- Windows (хост) → WSL2 (Ubuntu) → Docker → Контейнеры
- Доступ: VS Code Remote SSH
- Запуск: Автоматически при загрузке Windows

---

## Быстрая настройка

### 1. Установка (один раз)

Следуйте инструкции: **`REMOTE_WINDOWS_SETUP.md`** (разделы 1-8)

**Основные шаги:**
1. Установить WSL2 (Ubuntu 22.04)
2. Установить Docker Desktop (с WSL2 backend)
3. Настроить SSH в WSL2
4. Подключиться через VS Code Remote SSH
5. Клонировать проект
6. Запустить контейнеры

### 2. Настройка автозапуска (для работы 24/7)

#### Автозапуск Docker Desktop
1. Docker Desktop → Settings → General
2. ✅ **Start Docker Desktop when you log in**

#### Автозапуск контейнеров в WSL2

Добавить в `~/.bashrc` (в WSL2):
```bash
# Автозапуск сервисов при входе в WSL2
if command -v docker &> /dev/null && docker ps &> /dev/null 2>/dev/null; then
    cd ~/projects/invest-python-main/complex_flag_scanner 2>/dev/null && \
    docker compose up -d > /dev/null 2>&1
fi
```

Перезагрузить `.bashrc`:
```bash
source ~/.bashrc
```

### 3. Ежедневное использование

#### Утренний запуск (если компьютер был выключен)
1. Включить компьютер
2. Войти в Windows (Docker Desktop запустится автоматически)
3. Подключиться через VS Code Remote SSH
4. Проверить статус: `docker compose ps`

#### Проверка статуса
```bash
# В VS Code терминале (подключенном к WSL2)
cd ~/projects/invest-python-main/complex_flag_scanner

# Статус контейнеров
docker compose ps

# Логи торгового бота
docker compose logs -f trading-bot

# Использование ресурсов
docker stats
```

#### Запуск обучения
```bash
# В фоновой сессии (screen)
./scripts/start_training.sh 100 16 0.003

# Мониторинг
tail -f neural_network/training_log.txt
```

---

## Преимущества этого подхода

✅ **Не нужен отдельный сервер** - используете свой компьютер
✅ **Бесплатно** - нет затрат на VPS
✅ **Полный контроль** - все под рукой
✅ **VS Code интеграция** - удобная разработка
✅ **Автозапуск** - работает автоматически

---

## Ограничения

⚠️ **Компьютер должен быть включен** - бот не работает когда компьютер выключен
⚠️ **Зависит от интернета** - нужен стабильный доступ к API
⚠️ **Ресурсы компьютера** - обучение может замедлить систему

---

## Доступ к дашбордам

### Локальный доступ (на этом же компьютере)
После проброса портов через VS Code:
- **Разметка:** http://localhost:8505
- **Trading Dashboard:** http://localhost:8506

### Доступ с других устройств в локальной сети

Если нужно заходить с телефона/планшета в той же Wi-Fi сети:

1. **Узнать IP адрес Windows компьютера:**
   ```powershell
   # В PowerShell
   ipconfig
   # Ищите IPv4 Address (например, 192.168.1.100)
   ```

2. **Пробросить порты через SSH туннель:**
   ```bash
   # На другом устройстве (если есть SSH клиент)
   ssh -L 8505:localhost:8505 -L 8506:localhost:8506 user@192.168.1.100
   ```

3. **Или использовать Windows Firewall (для прямого доступа):**
   - Открыть порты 8505 и 8506 в Windows Firewall
   - Доступ: http://192.168.1.100:8505

---

## Резервное копирование

### Автоматический backup (опционально)

Создать скрипт для backup:
```bash
nano ~/backup_project.sh
```

Содержимое:
```bash
#!/bin/bash
DATE=$(date +%Y%m%d)
BACKUP_DIR=~/backups
mkdir -p $BACKUP_DIR

cd ~/projects/invest-python-main/complex_flag_scanner

# Backup аннотаций
tar -czf $BACKUP_DIR/annotations_$DATE.tar.gz neural_network/data/annotations.csv

# Backup моделей
tar -czf $BACKUP_DIR/models_$DATE.tar.gz neural_network/models/

# Удалить старые backup (старше 30 дней)
find $BACKUP_DIR -name "*.tar.gz" -mtime +30 -delete

echo "Backup completed: $DATE"
```

Добавить в cron (автоматический запуск раз в день):
```bash
crontab -e
```

Добавить строку:
```
0 2 * * * /home/yourname/backup_project.sh >> /home/yourname/backup.log 2>&1
```
(Запуск каждый день в 2:00 ночи)

---

## Мониторинг и уведомления

### Проверка работы бота

Создать скрипт для проверки:
```bash
nano ~/check_bot.sh
```

Содержимое:
```bash
#!/bin/bash
cd ~/projects/invest-python-main/complex_flag_scanner

# Проверить, что контейнеры запущены
if ! docker compose ps | grep -q "Up"; then
    echo "⚠️ Контейнеры не запущены!"
    docker compose up -d
fi
```

Запускать по расписанию (cron) или вручную.

---

## Полезные команды

```bash
# Перезапуск всех сервисов
docker compose restart

# Остановка всех сервисов
docker compose stop

# Обновление кода и перезапуск
git pull
docker compose build
docker compose up -d

# Очистка неиспользуемых ресурсов Docker
docker system prune -a

# Проверка свободного места
df -h
```

---

## Troubleshooting

### Контейнеры не запускаются после перезагрузки
**Причина:** WSL2 не запустился или Docker Desktop не запущен
**Решение:**
1. Проверить, что Docker Desktop запущен
2. В WSL2: `docker ps` должен работать
3. Если нет - перезапустить Docker Desktop

### Нет доступа к портам
**Причина:** Проброс портов через VS Code не работает
**Решение:**
1. Переподключиться через VS Code Remote SSH
2. Или использовать прямое подключение: `localhost:8505`

### Медленная работа
**Причина:** Нехватка ресурсов
**Решение:**
1. Проверить использование CPU/RAM: `docker stats`
2. Ограничить ресурсы в `docker-compose.yml`
3. Закрыть другие тяжелые программы

---

**Подробная инструкция:** См. `REMOTE_WINDOWS_SETUP.md`
