# 📦 Локальный перенос проекта на удаленный компьютер

Поскольку Git push не работает из-за больших файлов, можно перенести проект локально.

---

## 🎯 Вариант 1: Создание архива (рекомендуется)

### Шаг 1: Создать архив на текущем компьютере

```bash
cd complex_flag_scanner
./scripts/create_backup.sh
```

Скрипт создаст архив `invest-python-backup-YYYYMMDD-HHMMSS.tar.gz` в родительской директории.

### Шаг 2: Перенести архив

- **USB-флешка**: Скопируйте `.tar.gz` файл на флешку
- **Облачное хранилище**: Загрузите в Google Drive, Dropbox, Яндекс.Диск
- **Сеть**: Используйте `scp`:
  ```bash
  scp ../invest-python-backup-*.tar.gz user@remote-host:~/
  ```

### Шаг 3: Распаковать на удаленном компьютере (WSL2)

```bash
# В WSL2
cd ~
tar -xzf invest-python-backup-YYYYMMDD-HHMMSS.tar.gz
cd invest-python-main/complex_flag_scanner
```

---

## 🎯 Вариант 2: Прямой перенос через сеть (rsync/scp)

Если есть сетевой доступ к удаленному компьютеру:

```bash
cd complex_flag_scanner
./scripts/transfer_to_remote.sh user@remote-host:~/projects/
```

**Пример:**
```bash
# Если WSL2 доступен по сети
./scripts/transfer_to_remote.sh username@192.168.1.100:~/projects/

# Или если есть SSH туннель
./scripts/transfer_to_remote.sh username@windows-remote:/mnt/c/projects/
```

---

## 🎯 Вариант 3: Клонировать из Git на удаленном компьютере

Если в Git уже есть весь код (кроме моделей):

```bash
# На удаленном компьютере (WSL2)
cd ~
git clone https://github.com/ArkadiyShkolniy/cult_flag_scanner.git
cd cult_flag_scanner
git checkout feature/multi-timeframe

# Перейти в нужную директорию
cd complex_flag_scanner
```

**⚠️ Важно:** Модели нужно будет обучить на сервере (см. `TRAINING_MODELS.md`)

---

## ✅ Что делать после переноса

### 1. Настройка Git (если использовали архив)

```bash
cd ~/invest-python-main/complex_flag_scanner

# Вариант A: Инициализировать новый репозиторий
git init
git remote add origin https://github.com/ArkadiyShkolniy/cult_flag_scanner.git
git branch -M feature/multi-timeframe
git add .
git commit -m "Initial commit from local transfer"

# Вариант B: Клонировать и скопировать файлы поверх
# (если нужно сохранить историю Git)
```

### 2. Создать `.env` файл

```bash
cp .env.example .env  # Если есть пример
# Или создайте вручную с вашими токенами
```

### 3. Запустить Docker контейнеры

```bash
docker compose up -d
```

### 4. Обучить модели (см. `TRAINING_MODELS.md`)

```bash
# Keypoint Detection Model
docker compose exec labeling python3 neural_network/train_keypoints.py \
    --epochs 100 --batch_size 16 --tolerance_normalized 0.003

# Trading Model (опционально)
docker compose exec labeling python3 neural_network/generate_trading_dataset.py
docker compose exec labeling python3 neural_network/train_trading_model.py
```

---

## 📋 Что включено/исключено из архива

### ✅ Включено:
- Весь исходный код
- Конфигурационные файлы (docker-compose.yml, Dockerfile)
- Документация
- Размеченные данные (`neural_network/data/annotations.csv`)
- Скрипты

### ❌ Исключено:
- `.git` директория (слишком большая)
- Модели (`*.pth`, `*.pkl`) - будут обучены на сервере
- `.env` файлы (секреты, создайте вручную)
- Логи и временные файлы
- Кэш Python (`__pycache__`)

---

## 🔍 Проверка размера архива

```bash
# Размер архива
ls -lh invest-python-backup-*.tar.gz

# Обычно должен быть 10-50 MB (без моделей и .git)
```

---

## 💡 Рекомендации

1. **Используйте вариант 1 (архив)** - самый надежный и универсальный
2. **Сохраните `.env` файл отдельно** - не включайте его в архив
3. **После переноса проверьте** наличие всех файлов:
   ```bash
   ls -la neural_network/data/annotations.csv  # Должен быть
   ls -la docker-compose.yml  # Должен быть
   ```
