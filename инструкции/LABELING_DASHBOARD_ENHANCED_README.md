# Расширенный дашборд разметки паттернов

## Описание

Расширенный дашборд для разметки паттернов "Флаг" с использованием нейросети для автоматического поиска паттернов. Позволяет эффективно улучшать обучение модели через обратную связь пользователя.

## Основные возможности

1. **Автоматический поиск паттернов**: Нейросеть автоматически находит возможные паттерны на графике
2. **Интерактивное подтверждение**: Пользователь подтверждает или отклоняет найденные паттерны
3. **Улучшение обучения**: 
   - Подтвержденные паттерны сохраняются как положительные примеры (label=1 или 2)
   - Отклоненные паттерны сохраняются как отрицательные примеры (label=0)
4. **Гибкие настройки**: Выбор инструмента, периода, таймфрейма и параметров нейросети

## Запуск

```bash
cd complex_flag_scanner
streamlit run neural_network/labeling_dashboard_enhanced.py
```

## Использование

### 1. Загрузка данных

1. В боковой панели выберите:
   - **Тикер** (например, "T")
   - **Class Code** (TQBR, SPBFUT, FUT)
   - **Таймфрейм** (5m, 1h, 1d)
   - **Режим загрузки данных**:
     - По количеству дней
     - По датам (начальная и конечная дата)
2. Нажмите **"📥 Загрузить данные"**

### 2. Поиск паттернов

1. Настройте параметры нейросети:
   - **Путь к модели** (по умолчанию: `neural_network/models/keypoint_model_best.pth`)
   - **Размер окна** (по умолчанию: 100 свечей)
   - **Шаг окна** (по умолчанию: 10 свечей)
   - **Минимальная уверенность** (по умолчанию: 0.6)
2. Нажмите **"🔍 Найти паттерны (нейросеть)"**

### 3. Проверка и разметка паттернов

После поиска на графике будут отображены все найденные паттерны:

- **⏳ Ожидает решения** - паттерн найден, но еще не проверен (пунктирная линия)
- **✅ Подтвержден** - паттерн подтвержден пользователем (сплошная линия)
- **❌ Отклонен** - паттерн отклонен пользователем (точечная линия, полупрозрачный)

Для каждого паттерна:

1. **Просмотрите информацию**:
   - Тип паттерна (бычий/медвежий)
   - Вероятность предсказания
   - Координаты точек T0-T4
   - Результаты геометрических проверок

2. **Примите решение**:
   - Нажмите **"✅ Подтвердить"** - если паттерн правильный
   - Нажмите **"❌ Отклонить"** - если паттерн неправильный

### 4. Результаты

- **Подтвержденные паттерны** сохраняются с меткой:
  - `label=1` для бычьих паттернов
  - `label=2` для медвежьих паттернов
- **Отклоненные паттерны** сохраняются с меткой:
  - `label=0` (нет паттерна)

## Статистика

В нижней части дашборда отображается статистика:
- Общее количество размеченных данных
- Распределение по меткам (0, 1, 2)
- Распределение по таймфреймам

## Преимущества

1. **Эффективность**: Автоматический поиск паттернов экономит время
2. **Качество данных**: Пользователь проверяет каждый паттерн перед сохранением
3. **Улучшение модели**: Отрицательные примеры помогают модели лучше различать ложные срабатывания
4. **Гибкость**: Можно работать с любым инструментом и периодом

## Рекомендации

1. **Начните с высокого порога уверенности** (0.7-0.8) для получения только качественных паттернов
2. **Проверяйте геометрические нарушения** перед подтверждением
3. **Размечайте разнообразные данные**: разные инструменты, таймфреймы, периоды
4. **Регулярно переобучайте модель** на новых размеченных данных

## Технические детали

- Использует модель keypoint detection для поиска паттернов
- Применяет sliding window для анализа всего периода
- Сохраняет данные в `neural_network/data/annotations.csv`
- Свечи сохраняются в `neural_network/data/candles/`

## Сравнение с обычным дашбордом

| Функция | Обычный дашборд | Расширенный дашборд |
|---------|----------------|---------------------|
| Поиск паттернов | Вручную | Автоматически (нейросеть) |
| Разметка | Ручная | Подтверждение/отклонение |
| Отрицательные примеры | Нет | Да (label=0) |
| Эффективность | Низкая | Высокая |
| Улучшение модели | Медленное | Быстрое |
