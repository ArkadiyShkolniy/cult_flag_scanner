# Анализ блокировок открытия сделок

## Проблема
В обоих ботах (debug и prod) не проходят сделки, хотя паттерны находятся.

## Найденные причины

### 1. ✅ T4 устаревает до проверки условий входа
**Проблема:** Сканер находит паттерны на исторических данных, где T4 уже сформировалась несколько свечей назад (возраст 3+ свечей). Такие паттерны помечаются как "устаревшие" и пропускаются.

**Решение:** 
- Добавлен `PatternWatcher` для отслеживания паттернов без T4
- Когда сканер находит паттерн с устаревшей T4, но линии 1-3 и 2-4 параллельны, паттерн добавляется в отслеживание
- При каждом сканировании проверяется, сформировалась ли новая T4 для отслеживаемых паттернов
- Если T4 сформировалась на текущей/предыдущей свече, проверяются условия входа

### 2. ✅ Добавлена проверка отслеживаемых паттернов
**Изменения:**
- Перед анализом новых паттернов проверяются отслеживаемые паттерны
- Если T4 сформировалась для отслеживаемого паттерна, проверяются условия входа
- Если условия выполнены, открывается позиция

### 3. ✅ Улучшено логирование
**Добавлено:**
- Логирование проверки T4 (возраст свечей)
- Логирование добавления паттернов в отслеживание
- Логирование проверки отслеживаемых паттернов
- Логирование причин блокировки в TradeManager

## Текущая логика работы

1. **Сканер находит паттерн с T4:**
   - Если T4 актуальна (возраст ≤ 1 свечи) → проверяются условия входа
   - Если T4 устарела (возраст > 1 свечи) → проверяется параллельность линий 1-3 и 2-4
     - Если параллельны → паттерн добавляется в отслеживание

2. **Проверка отслеживаемых паттернов:**
   - При каждом сканировании проверяются все отслеживаемые паттерны
   - Если T4 сформировалась на текущей/предыдущей свече и лежит на параллельной линии → проверяются условия входа
   - Если условия выполнены → открывается позиция

3. **Условия входа:**
   - Parallel Lines: T4 на текущей/предыдущей свече, линии 1-3 и 2-4 параллельны, T4 ≤ T2 (LONG) или T4 ≥ T2 (SHORT)
   - EMA Squeeze: Цена между EMA и линией 1-3
   - Если хотя бы одно условие выполнено → вход разрешен

## Как проверить работу

1. **Смотрите логи:**
   ```bash
   tail -f logs/prod_bot.log
   tail -f logs/debug_bot.log
   ```

2. **Ищите сообщения:**
   - `🚩 флаг найден` - паттерн найден сканером
   - `⏭️ Паттерн устарел` - T4 устарела, паттерн может быть добавлен в отслеживание
   - `👁️ Начато отслеживание паттерна` - паттерн добавлен в отслеживание
   - `🎯 T4 сформировалась для отслеживаемого паттерна` - T4 найдена для отслеживаемого паттерна
   - `✅ СИГНАЛ НА ВХОД` - условия входа выполнены
   - `🔔 СИГНАЛ НА ВХОД` - TradeManager получил сигнал
   - `❌ Отмена` - причина блокировки

3. **Проверьте количество отслеживаемых паттернов:**
   - В логах должно быть: `📊 Отслеживаемых паттернов: N`

## Возможные проблемы

1. **T4 не формируется для отслеживаемых паттернов:**
   - Проверьте, что цена T4 близка к ожидаемой (на параллельной линии)
   - Допуск: 1% (tolerance_percent=0.01)

2. **Условия входа не выполняются:**
   - Проверьте логи с сообщением `❌ Условия входа не выполнены`
   - Проверьте, что линии действительно параллельны
   - Проверьте, что T4 находится в правильном положении относительно T2

3. **Паттерны не добавляются в отслеживание:**
   - Проверьте, что линии 1-3 и 2-4 (через T2) параллельны
   - Проверьте логи с сообщением `👁️ Начато отслеживание паттерна`
