# 🍎 Быстрый доступ к дашбордам с Mac

## Текущая ситуация

✅ Порты открыты: 8502, 8505, 8506
✅ IP адрес Windows хоста: `172.18.112.1`

## Быстрый доступ

Выполните в WSL2:
```bash
cd /home/ark/projects/trading_bot
./get-windows-ip.sh
```

Скрипт покажет все необходимые URL.

## Прямые ссылки

Если IP адрес Windows хоста: `172.18.112.1`, то:

- 🎨 **Инструмент разметки:** http://172.18.112.1:8505
- 🤖 **Дашборд отладки:** http://172.18.112.1:8506
- 💰 **Дашборд продакшена:** http://172.18.112.1:8502

## Если не работает

### Вариант 1: Используйте IP адрес Windows в локальной сети

1. В Windows откройте PowerShell и выполните:
   ```powershell
   ipconfig | findstr IPv4
   ```

2. Найдите IP адрес вашего сетевого адаптера (обычно `192.168.x.x`)

3. Используйте этот IP на Mac:
   - http://192.168.x.x:8505
   - http://192.168.x.x:8506
   - http://192.168.x.x:8502

### Вариант 2: Настройте Windows Firewall

1. Откройте **Windows Defender Firewall**
2. **Advanced settings** → **Inbound Rules** → **New Rule**
3. Выберите **Port** → **TCP** → порты: `8502, 8505, 8506`
4. Разрешите подключение
5. Примените для всех профилей

### Вариант 3: Проверьте, что контейнеры запущены

```bash
cd /home/ark/projects/trading_bot
docker ps
```

Если контейнеры не запущены:
```bash
./docker-start.sh
```

## Проверка доступности

На Mac выполните:
```bash
curl http://172.18.112.1:8505
# или с IP локальной сети
curl http://192.168.x.x:8505
```

Если получаете ответ - порт доступен!
