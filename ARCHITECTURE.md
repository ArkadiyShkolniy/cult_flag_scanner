# Архитектура проекта: 3 основные сущности

Проект состоит из трех независимых компонентов, каждый со своей задачей:

## 1. 🎨 Инструмент для разметки паттернов (Labeling Tool)

**Назначение:** Ручная и автоматическая разметка паттернов "Флаг" для обучения нейросети

**Файлы:**
- `neural_network/labeling_dashboard.py` - Streamlit дашборд для разметки
- `neural_network/annotator.py` - Класс для аннотации паттернов
- `neural_network/check_annotations_geometry.py` - Проверка геометрии паттернов

**Запуск:**
```bash
# Локально
streamlit run neural_network/labeling_dashboard.py --server.port=8505

# Через Docker
docker-compose up labeling
```

**Доступ:** http://localhost:8505

**Функционал:**
- Ручная разметка точек T0-T4 на графиках
- Автоматический поиск паттернов с помощью нейросети
- Подтверждение/отклонение найденных паттернов
- Редактирование существующих аннотаций
- Исправление записей с нарушениями геометрии
- Экспорт данных для обучения ML моделей

**Данные:**
- `neural_network/data/annotations.csv` - база разметки
- `neural_network/data/candles/` - кэш свечей
- `neural_network/models/` - обученные модели

---

## 2. 🤖 Торговый робот для отладки (Debug Trading Bot)

**Назначение:** Тестирование торговых стратегий без реальных денег (Paper Trading)

**Файлы:**
- `service.py` - основной сервис сканирования и торговли
- `trading_bot/trade_manager.py` - управление сделками
- `trading_bot/trade_strategy.py` - стратегия входа
- `trading_bot/trading_dashboard.py` - дашборд для мониторинга

**Запуск:**
```bash
# Локально (режим отладки)
python service.py --mode debug --enable-trading

# Через Docker
docker-compose up trading-bot trading-dashboard
```

**Доступ:**
- Дашборд: http://localhost:8506

**Параметры:**
- `dry_run=True` - эмуляция торгов (без реальных денег)
- `debug_mode=True` - всегда торговать 1 лотом
- `data_dir=trading_bot` - директория для данных отладки
- `use_ai_filter=True` - использовать ML модель для фильтрации

**Данные:**
- `trading_bot/trades_active.json` - активные позиции
- `trading_bot/trades_history.json` - история сделок
- `trading_bot/training_data/` - данные для обучения ML

**Режимы входа:**
- `--entry-mode ema_squeeze` - вход между EMA и линией тренда
- `--entry-mode parallel_lines` - вход при параллельности линий 1-3 и 2-4

---

## 3. 💰 Торговый робот на реальном рынке (Production Trading Bot)

**Назначение:** Автоматическая торговля на реальном рынке с реальными деньгами

**Файлы:**
- `service.py` - основной сервис (режим PROD)
- `trading_bot/trade_manager.py` - управление сделками (dry_run=False)
- `trading_bot/trade_strategy.py` - стратегия входа
- `trading_bot/trading_dashboard.py` - дашборд для мониторинга
- `docker-compose.prod.yml` - конфигурация для продакшена

**Запуск:**
```bash
# Локально (режим продакшена)
python service.py --mode prod --entry-mode parallel_lines --enable-trading

# Через Docker
docker-compose -f docker-compose.prod.yml up scanner_prod dashboard_prod
```

**Доступ:**
- Дашборд: http://localhost:8502

**Параметры:**
- `dry_run=False` - **РЕАЛЬНАЯ ТОРГОВЛЯ** (с реальными деньгами!)
- `data_dir=trading_bot/data_prod` - отдельная директория для продакшена
- `use_ai_filter=True` - использовать ML модель для фильтрации
- Telegram уведомления включены (если настроены)

**Данные:**
- `trading_bot/data_prod/trades_active.json` - активные позиции
- `trading_bot/data_prod/trades_history.json` - история сделок
- `trading_bot/data_prod/training_data/` - данные для обучения ML

**⚠️ ВАЖНО:**
- Перед запуском убедитесь, что все параметры настроены правильно
- Проверьте токен API и права доступа
- Начните с минимальных объемов
- Мониторьте работу бота через дашборд

---

## Сравнительная таблица

| Параметр | Разметка | Отладка | Продакшен |
|----------|----------|---------|-----------|
| **Назначение** | Обучение ML | Тестирование | Реальная торговля |
| **Деньги** | Нет | Нет (dry_run) | **ДА (реальные)** |
| **Порт дашборда** | 8505 | 8506 | 8502 |
| **Директория данных** | `neural_network/data/` | `trading_bot/` | `trading_bot/data_prod/` |
| **Telegram** | Нет | Нет | Да |
| **Режим сканирования** | История | История + свежие | Только свежие |
| **Режим входа** | - | ema_squeeze/parallel_lines | parallel_lines |

---

## Взаимосвязь компонентов

```
Разметка паттернов
    ↓ (обучает модели)
ML Модели (neural_network/models/)
    ↓ (используются для фильтрации)
Торговый робот (отладка)
    ↓ (тестирование стратегий)
Торговый робот (продакшен)
```

---

## Рекомендации по использованию

1. **Сначала** используйте инструмент разметки для создания обучающей выборки
2. **Затем** обучите ML модели на размеченных данных
3. **Потом** тестируйте стратегии в режиме отладки (dry_run)
4. **Только после** успешного тестирования запускайте продакшен

---

## Безопасность

- ✅ Отладка и продакшен используют **разные директории** данных
- ✅ Продакшен требует явного указания `--mode prod`
- ✅ Все сделки логируются для анализа
- ✅ ML фильтрация снижает риск убыточных сделок
