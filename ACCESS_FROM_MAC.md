# 🍎 Доступ к дашбордам с Mac

## Проблема

Дашборды запущены в WSL2 на Windows, а вы хотите открыть их в браузере на Mac. WSL2 работает в виртуальной сети, поэтому нужна настройка для доступа извне.

## Решения

### Вариант 1: Port Forwarding через Windows (рекомендуется)

WSL2 автоматически пробрасывает порты на localhost Windows, но нужно настроить доступ из сети.

#### Шаг 1: Найдите IP адрес Windows хоста

В WSL2 выполните:
```bash
cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
```

Это будет IP адрес Windows хоста в сети WSL2 (обычно что-то вроде `172.x.x.1`).

#### Шаг 2: Настройте firewall в Windows

1. Откройте **Windows Defender Firewall** в Windows
2. Нажмите **Advanced settings**
3. Создайте **Inbound Rule** для портов:
   - 8502 (продакшен дашборд)
   - 8505 (инструмент разметки)
   - 8506 (отладка дашборд)
4. Разрешите подключения из сети

#### Шаг 3: Найдите IP адрес Windows в локальной сети

В Windows выполните в PowerShell:
```powershell
ipconfig | findstr IPv4
```

Найдите IP адрес вашего сетевого адаптера (обычно начинается с `192.168.` или `10.`).

#### Шаг 4: Откройте в браузере на Mac

Используйте IP адрес Windows в локальной сети:

- 🎨 **Инструмент разметки:** `http://<WINDOWS_IP>:8505`
- 🤖 **Дашборд отладки:** `http://<WINDOWS_IP>:8506`
- 💰 **Дашборд продакшена:** `http://<WINDOWS_IP>:8502`

### Вариант 2: Использование localhost (если Mac и Windows в одной сети)

Если Mac и Windows в одной локальной сети, можно использовать IP адрес Windows напрямую.

#### Шаг 1: Узнайте IP адрес Windows

В Windows PowerShell:
```powershell
ipconfig
```

Найдите IPv4 адрес (например, `192.168.1.100`).

#### Шаг 2: Откройте в браузере Mac

- 🎨 **Инструмент разметки:** `http://192.168.1.100:8505`
- 🤖 **Дашборд отладки:** `http://192.168.1.100:8506`
- 💰 **Дашборд продакшена:** `http://192.168.1.100:8502`

### Вариант 3: SSH туннель (самый безопасный)

#### Шаг 1: Настройте SSH в WSL2

```bash
# В WSL2
sudo apt install openssh-server
sudo service ssh start
```

#### Шаг 2: Создайте SSH туннель с Mac

На Mac выполните:
```bash
ssh -L 8505:localhost:8505 -L 8506:localhost:8506 -L 8502:localhost:8502 <USER>@<WINDOWS_IP>
```

#### Шаг 3: Откройте в браузере Mac

- 🎨 **Инструмент разметки:** `http://localhost:8505`
- 🤖 **Дашборд отладки:** `http://localhost:8506`
- 💰 **Дашборд продакшена:** `http://localhost:8502`

### Вариант 4: Использование ngrok (для внешнего доступа)

Если нужен доступ из интернета:

```bash
# Установите ngrok
# На Windows или в WSL2
ngrok http 8505
ngrok http 8506
ngrok http 8502
```

Используйте предоставленные ngrok URL.

## Быстрая проверка

### Проверка портов в WSL2

```bash
# Проверка, что порты открыты
netstat -tuln | grep -E "8502|8505|8506"
# или
ss -tuln | grep -E "8502|8505|8506"
```

### Проверка контейнеров

```bash
docker ps
# Должны быть видны контейнеры с портами 8502, 8505, 8506
```

### Проверка доступности с Mac

```bash
# На Mac
curl http://<WINDOWS_IP>:8505
curl http://<WINDOWS_IP>:8506
curl http://<WINDOWS_IP>:8502
```

## Автоматический скрипт для получения IP

Создайте скрипт `get-windows-ip.sh`:

```bash
#!/bin/bash
echo "IP адрес Windows хоста (для WSL2):"
cat /etc/resolv.conf | grep nameserver | awk '{print $2}'
echo ""
echo "Для доступа с Mac используйте этот IP адрес"
echo "Пример: http://$(cat /etc/resolv.conf | grep nameserver | awk '{print $2}'):8505"
```

## Важные замечания

⚠️ **Безопасность:**
- Открытие портов в firewall может быть небезопасно
- Используйте SSH туннель для безопасного доступа
- Не открывайте порты в публичной сети без защиты

⚠️ **Firewall:**
- Убедитесь, что Windows Firewall разрешает входящие подключения
- Проверьте настройки антивируса

## Устранение проблем

### Проблема: Не могу подключиться

1. Проверьте, что контейнеры запущены: `docker ps`
2. Проверьте, что порты открыты: `netstat -tuln | grep 8505`
3. Проверьте firewall в Windows
4. Убедитесь, что Mac и Windows в одной сети

### Проблема: Connection refused

1. Проверьте, что Streamlit слушает на `0.0.0.0`, а не `127.0.0.1`
2. В `docker-compose.all.yml` должно быть `--server.address=0.0.0.0`
3. Проверьте настройки портов в docker-compose

### Проблема: Timeout

1. Проверьте сетевые настройки
2. Убедитесь, что нет блокировки на уровне роутера
3. Попробуйте ping между Mac и Windows
