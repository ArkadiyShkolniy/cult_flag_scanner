# 🐳 Настройка Docker для хранения данных на диске E

## Зачем это нужно?

По умолчанию Docker хранит все данные (образы, контейнеры, volumes) на диске C, который может быть переполнен. Настройка Docker для использования диска E освободит место на диске C и обеспечит больше места для работы.

## Текущая ситуация

- **Диск C:** 395G / 466G (85% заполнен) - только 71G свободно
- **Диск E:** 268G / 932G (29% заполнен) - 665G свободно
- **Проект:** 729M на диске E

## Способы настройки

### Способ 1: Через скрипт (рекомендуется для Linux/WSL)

```bash
cd /home/ark/projects/trading_bot
chmod +x setup-docker-on-e.sh
sudo ./setup-docker-on-e.sh
```

Скрипт:
- Создаст директорию `/mnt/e/docker`
- Настроит Docker daemon для использования диска E
- При необходимости переместит существующие данные

### Способ 2: Ручная настройка (для WSL2)

#### Шаг 1: Создайте директорию на диске E

```bash
sudo mkdir -p /mnt/e/docker/{containers,images,volumes,network,swarm,overlay2,plugins}
sudo chmod -R 755 /mnt/e/docker
```

#### Шаг 2: Остановите Docker

```bash
sudo systemctl stop docker
# или
sudo service docker stop
```

#### Шаг 3: Настройте daemon.json

```bash
sudo mkdir -p /etc/docker
sudo nano /etc/docker/daemon.json
```

Добавьте:
```json
{
  "data-root": "/mnt/e/docker",
  "storage-driver": "overlay2"
}
```

#### Шаг 4: Переместите существующие данные (опционально)

```bash
# Создайте резервную копию
sudo mv /var/lib/docker /var/lib/docker.backup

# Скопируйте данные
sudo rsync -av /var/lib/docker.backup/ /mnt/e/docker/
```

#### Шаг 5: Запустите Docker

```bash
sudo systemctl start docker
# или
sudo service docker start
```

#### Шаг 6: Проверьте

```bash
docker info | grep "Docker Root Dir"
# Должно показать: /mnt/e/docker
```

### Способ 3: Через Docker Desktop (для Windows)

Если используется Docker Desktop:

1. Откройте Docker Desktop
2. Перейдите в **Settings** → **Resources** → **Advanced**
3. В разделе **Disk image location** укажите путь на диске E:
   ```
   E:\Docker
   ```
4. Нажмите **Apply & Restart**

## Проверка

После настройки проверьте:

```bash
# Проверка расположения данных
docker info | grep "Docker Root Dir"

# Проверка использования диска
df -h /mnt/e/docker

# Тестовый запуск контейнера
docker run hello-world
```

## Преимущества

✅ **Больше места:** 665G свободно на диске E vs 71G на диске C
✅ **Лучшая производительность:** Диск E менее загружен
✅ **Единое расположение:** Проект и Docker на одном диске

## Важные замечания

⚠️ **Внимание:**
- Перед настройкой убедитесь, что Docker остановлен
- Создайте резервную копию существующих данных
- После настройки проверьте работу Docker

## Откат изменений

Если нужно вернуть настройки по умолчанию:

```bash
sudo systemctl stop docker
sudo rm /etc/docker/daemon.json
sudo mv /var/lib/docker.backup /var/lib/docker  # если создавали backup
sudo systemctl start docker
```

## Размер данных Docker

После настройки можно проверить размер:

```bash
du -sh /mnt/e/docker
```

Обычно Docker занимает:
- Базовые образы: 100-500MB
- Образы проекта: 500MB-2GB
- Контейнеры: зависит от использования
- Volumes: зависит от данных приложения
