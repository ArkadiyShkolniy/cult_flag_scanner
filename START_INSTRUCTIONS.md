# 📋 Инструкция по запуску всех компонентов

## Текущая ситуация

Все скрипты для запуска созданы, но компоненты не запустились из-за отсутствия зависимостей Python.

## Что нужно сделать

### Шаг 1: Установите зависимости

```bash
cd /home/ark/projects/trading_bot

# Установите pip, если его нет
sudo apt update
sudo apt install python3-pip -y

# Установите зависимости проекта
python3 -m pip install --user -r requirements.txt

# Или установите основные зависимости вручную:
python3 -m pip install --user streamlit pandas plotly matplotlib python-dotenv numpy scikit-learn joblib requests tqdm
```

### Шаг 2: Проверьте файл .env

Убедитесь, что файл `.env` существует и содержит токен:
```bash
cat .env
# Должно быть:
# TINKOFF_INVEST_TOKEN=your_token_here
```

### Шаг 3: Запустите компоненты

#### Вариант A: Используйте скрипт (после установки зависимостей)
```bash
./start_all.sh
```

#### Вариант B: Запустите вручную в отдельных терминалах

**Терминал 1 - Инструмент разметки:**
```bash
cd /home/ark/projects/trading_bot
streamlit run neural_network/labeling_dashboard.py --server.port=8505 --server.address=0.0.0.0
```

**Терминал 2 - Торговый робот отладки:**
```bash
cd /home/ark/projects/trading_bot
python3 service.py --mode debug --enable-trading --entry-mode parallel_lines
```

**Терминал 3 - Дашборд отладки:**
```bash
cd /home/ark/projects/trading_bot
streamlit run trading_bot/trading_dashboard.py --server.port=8506 --server.address=0.0.0.0
```

**Терминал 4 - Торговый робот продакшена:**
```bash
cd /home/ark/projects/trading_bot
python3 service.py --mode prod --enable-trading --entry-mode parallel_lines
```

**Терминал 5 - Дашборд продакшена:**
```bash
cd /home/ark/projects/trading_bot
streamlit run trading_bot/trading_dashboard.py --server.port=8502 --server.address=0.0.0.0
```

## Проверка статуса

После запуска проверьте статус:
```bash
./status.sh
```

## Доступ к компонентам

После успешного запуска откройте в браузере:

- 🎨 **Инструмент разметки:** http://localhost:8505
- 🤖 **Дашборд отладки:** http://localhost:8506
- 💰 **Дашборд продакшена:** http://localhost:8502

## Остановка компонентов

```bash
./stop_all.sh
```

## Созданные файлы

✅ `start_all.sh` - скрипт для запуска всех компонентов
✅ `stop_all.sh` - скрипт для остановки всех компонентов
✅ `status.sh` - скрипт для проверки статуса
✅ `QUICKSTART.md` - краткая инструкция
✅ `ARCHITECTURE.md` - описание архитектуры проекта

## Важно

⚠️ **Торговый робот продакшена работает с реальными деньгами!**
- Убедитесь, что все параметры настроены правильно
- Начните с минимальных объемов
- Мониторьте работу через дашборд
