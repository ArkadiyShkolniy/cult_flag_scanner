# Диагностика блокировок открытия сделок

## Что было проверено и исправлено:

### 1. ✅ Добавлено детальное логирование
   - Логирование проверки T4 (возраст свечей)
   - Логирование проверки условий входа
   - Логирование причин блокировки в TradeManager
   - Логирование расчета объема позиции

### 2. ✅ Исправлена обработка AI фильтра
   - Если AI модель не найдена, торговля продолжается БЕЗ фильтра
   - Добавлено предупреждение в логах

### 3. ✅ Исправлена инициализация TradeManager
   - Правильная передача debug_mode для prod режима
   - Правильная передача use_ai_filter из переменных окружения

### 4. ✅ Добавлены проверки перед открытием позиции
   - Проверка enable_trading
   - Проверка инициализации TradeManager
   - Проверка инициализации Strategy

## Возможные причины блокировки:

1. **Паттерны не находятся** - сканер не находит подходящие паттерны
2. **T4 устарела** - T4 находится более чем на 1 свечу назад (t4_age > 1)
3. **Условия входа не выполняются** - Parallel Lines или EMA Squeeze не срабатывают
4. **AI фильтр блокирует** - если модель загружена и вероятность < 50%
5. **Недостаточно капитала** - calculate_quantity возвращает 0 лотов
6. **Позиция уже открыта** - для этого тикера уже есть активная позиция

## Как проверить:

1. Смотрите логи: `tail -f logs/prod_bot.log`
2. Ищите сообщения:
   - `🚩 флаг найден` - паттерн найден
   - `📍 Проверка T4` - проверка возраста T4
   - `✅ T4 актуальна` - T4 на текущей/предыдущей свече
   - `✅ СИГНАЛ НА ВХОД` - условия входа выполнены
   - `🔔 СИГНАЛ НА ВХОД` - TradeManager получил сигнал
   - `❌ Отмена` - причина блокировки
   - `⚠️` - предупреждения

3. Запустите скрипт проверки: `python3 check_trading_blocks.py`
